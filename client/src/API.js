import axios from 'axios';

const BASE_URL = 'https://localhost500/api/';

const apiSettings = {
  checkUniqueUser: async (user) => {
    // Check if user is unique
  },
};

export default apiSettings;
